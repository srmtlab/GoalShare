/**
 * 
 */

function whiteBoard(){
	this.objects.drawables = new Array();
}

// Configuration
whiteBoard.prototype.config = {
	width: 800,
	height: 600,
	
	debug: 1
};

// State information
whiteBoard.prototype.state = {
		
};

//
whiteboard.prototype.objects = {
		
};
